# Provision AWS EKS cluster
apiVersion: xl-deploy/v1
kind: Infrastructure
spec:
- name: scott-micro-again
  type: core.Directory
  children:
  - name: aws-scott-micro-again
    type: aws.Cloud
    accesskey: !value AWSAccessKey
    accessSecret: !value AWSAccessSecret
    children:
    - name: eks-cloudformation
      type: aws.cloudformation.Stack
      region: us-east-2
---
apiVersion: xl-deploy/v1
kind: Environments
spec:
- name: scott-micro-again
  type: core.Directory
  children:
  - name: aws-cloudformation-scott-micro-again
    type: udm.Environment
    members:
    - Infrastructure/scott-micro-again/aws-scott-micro-again
    - Infrastructure/scott-micro-again/aws-scott-micro-again/eks-cloudformation