# Provision AWS EKS cluster
apiVersion: xl-deploy/v1
kind: Infrastructure
spec:
- name: scott-micro-move
  type: core.Directory
  children:
  - name: aws-scott-micro-move
    type: aws.Cloud
    accesskey: !value AWSAccessKey
    accessSecret: !value AWSAccessSecret
    children:
    - name: eks-cloudformation
      type: aws.cloudformation.Stack
      region: us-east-2
---
apiVersion: xl-deploy/v1
kind: Environments
spec:
- name: scott-micro-move
  type: core.Directory
  children:
  - name: aws-cloudformation-scott-micro-move
    type: udm.Environment
    members:
    - Infrastructure/scott-micro-move/aws-scott-micro-move
    - Infrastructure/scott-micro-move/aws-scott-micro-move/eks-cloudformation